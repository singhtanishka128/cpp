#include <bits/stdc++.h>
using namespace std;


int main() {
    // Starting number
    int a = 2;

    // Common difference
    int d = 1;

    // Nth term to find
    int N = 5;

    // Calculate the Nth term
    int ans = (a + (N - 1) * d);

   
    cout << ans;

    return 0;
}